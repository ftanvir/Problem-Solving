#include <bits/stdc++.h>

using namespace std;

int main()
{
    int a, b;
    scanf("%d %d", &a, &b);
    int m = int(strlen(a));
    int n = int (strlen(b));

    cout<<n<<" "<<m<<endl;
    /*if(a==b) {
        printf("=");
    }
    else if(a>b) {
        printf(">");
    }
    else {
        printf("<");
    }*/
    return 0;
}
